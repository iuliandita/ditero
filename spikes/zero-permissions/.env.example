# Copy to .env (gitignored). Dev-only values, no real secrets committed.
DATABASE_URL=postgres://postgres:pass@localhost:5432/ditero_spike
ZERO_AUTH_SECRET=spike-dev-secret-change-me
ZERO_CACHE_URL=http://localhost:4848
API_PORT=3000

# Spike B channels
NTFY_SERVER=https://ntfy.sh
NTFY_TOPIC=change-me-to-a-random-topic
# Telegram: create a bot via @BotFather, DM it once, then resolve chat id.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
